package linkedlist;

import linkedlist.double_linkedlist.LinkedList;

public class TestDoubleLinkedList {
      public static void main(String[] args) {

            LinkedList<Integer> list = new LinkedList<>();

            list.pushBack(10);
            list.pushBack(20);
            list.pushFront(30);
            list.pushFront(40);

      }
}
